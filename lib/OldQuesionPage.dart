import 'package:flutter/material.dart';
class OldQuestionPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("old question here"),
      ),
    );
  }
}